---
name: features
description: Extract features and user benefits from a codebase with evidence-based scanning across 10 signal categories and persona inference.
---

Scan this codebase for features across 10 signal categories (exports, CLI args, config files, API routes, tests, dependencies, CI/CD, docs, security, and architecture). Extract features with file-level evidence and translate each into a user benefit using the pattern: [Feature] so you can [outcome] — [evidence].

For the full PitchDocs plugin with 15 skills and 13 commands: https://github.com/littlebearapps/pitchdocs
