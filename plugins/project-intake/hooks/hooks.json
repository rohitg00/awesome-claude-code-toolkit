{
  "hooks": [
    {
      "type": "PostToolUse",
      "matcher": "Bash",
      "description": "Announce a completed project-intake handoff so the orchestrator proceeds to the next step",
      "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/announce-handoff.js"
    }
  ]
}
